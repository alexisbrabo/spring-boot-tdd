CREATE SEQUENCE person_id_seq as BIGINT START WITH 1 INCREMENTS BY 1;
CREATE TABLE person (
	code BIGINT GENERATED BY DEFAULT as SEQUENCE person_id_seq PRIMARY KEY,
	name VARCHAR(80) NOT NULL,
	cpf VARCHAR(11) NOT NULL
);